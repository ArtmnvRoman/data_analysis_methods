---
title: "hw01"
author: "Artamonov Roman"
date: ""
output:
  word_document: default
  pdf_document: default
  html_document:
    df_print: paged
---
```{r}
data.df <- read.table("http://people.math.umass.edu/~anna/Stat597AFall2016/rnf6080.dat", header = FALSE)
```



# Сколько строк и столбцов в data.df
```{r}
dim(data.df)
```



# Имена колонок
```{r}
colnames(data.df)
```


# Значение из 5 строки седьмого столбца
```{r}
data.df[5, 7]
```

# Печать 2 строки целиком
```{r}
data.df[2, ]
```



# Переименование колонок
```{r}
names(data.df) <- c("year", "month", "day", seq(0, 23))

```


# Просмотр начала таблицы
```{r}
head(data.df)
```


# Просмотр конца таблицы
```{r}
tail(data.df)
```


# Добавляем новую колонку daily с суммой значений в последних 24 колонках
```{r}
data.df$daily <- rowSums(data.df[, 4:27])
```



# Построение гистограммы
```{r}
hist(data.df$daily, main = "Гистограмма", xlab = "Сумма осадков")


fixed.df <- data.df
fixed.df[fixed.df$daily < 0, "daily"] <- 0
```



# Построение новой гистограммы
```{r}
hist(fixed.df$daily, main = "Новая гистограмма", xlab = "Сумма осадков")
```


# Синтаксис и типизирование
Вектор v - вектор состоящий из строковых значений
```{r}
v <- c("4", "8", "15", "16", "23", "42")
max(v) # Функция max() возвращает максимальное значение. В нашем случае поиск происходит на основе с лексикографическим порядком
sort(v) # Функция sort() отсортирует строки в лексикографическом порядке
# Функция sum() не может скалдывать строки
```
# Задание с вектором v2
```{r}
v2 <- c("5",7,12)
# v2[2] + 2[3] Ошибка в выражении 2[3]. Целые числа не индексируются
df3 <- data.frame(z1="5",z2=7,z3=12)
df3[1,2] + df3[1,3] # Значение во втором столбце первой строки — это 7, а в третьем столбце первой строки — это 12. Сложение этих двух значений даст: 7 + 12 = 19


l4 <- list(z1="6", z2=42, z3="49", z4=126)
l4[[2]] + l4[[4]] # l4[[2]] — это второй элемент списка, равный 42, и l4[[4]] — это четвертый элемент списка, равный 126. Сложение этих значений даст: 42 + 126 = 168

# l4[2] + l4[4] Возвращают подсписки, а не отдельные элементы. Попытка сложить два списка вызовет ошибку.
```

# Работа с функциями и операторами

# Числа от 1 до 10000 с инкрементом 372
```{r}
seq(1, 10000, by = 372) 
```

# Числа от 1 до 10000 длиной 50
```{r}
seq(1, 10000, length.out = 50)
```

# Функция rep(1:5, times = 3) повторяет весь вектор 1:5 три раза
```{r}
rep(1:5, times = 3)
```

# Функция rep(1:5, each = 3) повторяет каждый элемент вектора 1:5 по три раза.
```{r}
rep(1:5, each = 3)
```



